---
layout: page
navbar: false
sidebar: false
aside: false
outline: false
footer: false
title: "成都极智软创科技 - 专业软件企业服务商"
description: 成都极智软创科技是专业软件企业服务商。深耕软件开发、IT咨询、网站建设与企业组网，为企业提供一站式数字化解决方案，助力业务高效增长，用技术驱动企业数字化升级。
---

<!-- ==================== 导航栏 ==================== -->
<nav class="navbar" id="navbar">
    <div class="navbar-inner">
        <a href="#" class="nav-logo">
            <span class="logo-icon">G</span> 成都极智软创科技
        </a>
        <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="菜单">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round">
                <line x1="4" y1="6" x2="20" y2="6"></line>
                <line x1="4" y1="12" x2="20" y2="12"></line>
                <line x1="4" y1="18" x2="20" y2="18"></line>
            </svg>
        </button>
        <ul class="nav-links" id="navLinks">
            <li><a href="#services">服务项目</a></li>
            <li><a href="#features">核心优势</a></li>
            <li><a href="#process">合作流程</a></li>
            <li><a href="/cloud.html">极智云</a></li>
            <li><a href="#cta" class="nav-cta-btn">免费咨询</a></li>
        </ul>
    </div>
</nav>

<!-- ==================== 英雄区 ==================== -->
<section class="hero" id="hero">
    <div class="hero-inner">
        <div class="hero-text">
            <span class="hero-badge">🚀 专业软件企业服务商</span>
            <h1>用<span class="highlight">技术</span>驱动<br>企业数字化升级</h1>
            <p>深耕软件开发、IT咨询、网站建设与企业组网，为企业提供一站式数字化解决方案，助力业务高效增长。</p>
            <div class="hero-buttons">
                <a href="#services" class="btn btn-primary">查看服务项目</a>
                <a href="#cta" class="btn btn-outline">预约演示</a>
            </div>
        </div>
        <div class="hero-visual">
            <div class="hero-card-stack">
                <div class="hero-floating-card card-1">
                    <div class="card-icon blue">📱</div>
                    <h4>小程序开发</h4>
                    <span>微信/支付宝/抖音全平台</span>
                </div>
                <div class="hero-floating-card card-2">
                    <div class="card-icon green">🌐</div>
                    <h4>企业官网建设</h4>
                    <span>品牌形象·SEO优化</span>
                </div>
                <div class="hero-floating-card card-3">
                    <div class="card-icon purple">🔒</div>
                    <h4>企业组网服务</h4>
                    <span>安全·稳定·高效</span>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ==================== 服务项目 ==================== -->
<section class="section" id="services">
    <div class="section-inner">
        <div class="section-header">
            <span class="section-label">核心服务</span>
            <h2 class="section-title">全方位软件企业服务</h2>
            <p class="section-subtitle">从软件开发到IT基础设施，我们提供覆盖企业全生命周期的数字化服务。</p>
        </div>
        <div class="services-grid">
            <!-- 服务：SaaS软件服务 -->
            <div class="service-card card-saas">
                <div class="card-icon-wrap icon-saas">🧩</div>
                <h3><a href="/cloud.html">SaaS软件服务--极智云</a></h3>
                <p class="card-desc">提供开箱即用的超级图册、表单引擎、仪表盘引擎、流程引擎及零代码平台，快速搭建业务应用。</p>
                <div class="tag-list">
                    <a class="tag" href="/album-plus/">超级图册</a>
                    <a class="tag" href="/form-engine/">表单引擎</a>
                    <a class="tag" href="/dash-engine/">仪表盘引擎</a>
                    <a class="tag" href="/flow/">流程引擎</a>
                    <a class="tag" href="/zerocode/">零代码平台</a>
                </div>
                <a href="/cloud.html" class="card-link">
                    了解详情
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
            </div>
            <!-- 服务：软件开发 -->
            <div class="service-card card-dev">
                <div class="card-icon-wrap icon-dev">💻</div>
                <h3>软件开发服务</h3>
                <p class="card-desc">定制化开发小程序、APP、网站、H5页面及企业信息化系统，满足各行业业务需求。</p>
                <div class="tag-list">
                    <span class="tag">微信小程序</span>
                    <span class="tag">APP开发</span>
                    <span class="tag">H5页面</span>
                    <span class="tag">信息化系统</span>
                    <span class="tag">Web应用</span>
                </div>
                <a href="#cta" class="card-link">
                    了解详情
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
            </div>
            <!-- 服务：IT咨询 -->
            <div class="service-card card-consult">
                <div class="card-icon-wrap icon-consult">🎯</div>
                <h3>IT咨询服务</h3>
                <p class="card-desc">提供专业的软件解决方案咨询与企业IT问题诊断，帮助您做出明智的技术决策。</p>
                <div class="tag-list">
                    <span class="tag">方案咨询</span>
                    <span class="tag">技术选型</span>
                    <span class="tag">架构评审</span>
                    <span class="tag">IT诊断</span>
                    <span class="tag">数字化转型</span>
                </div>
                <a href="#cta" class="card-link">
                    了解详情
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
            </div>
            <!-- 服务：企业网站建设 -->
            <div class="service-card card-web">
                <div class="card-icon-wrap icon-web">🏢</div>
                <h3>企业网站建设</h3>
                <p class="card-desc">打造高端企业官网，提升品牌形象，集成SEO优化与数据分析，有效推广企业品牌。</p>
                <div class="tag-list">
                    <span class="tag">企业官网</span>
                    <span class="tag">品牌展示</span>
                    <span class="tag">SEO优化</span>
                    <span class="tag">响应式设计</span>
                    <span class="tag">多语言站</span>
                </div>
                <a href="#cta" class="card-link">
                    了解详情
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
            </div>
            <!-- 服务：企业组网 -->
            <div class="service-card card-network">
                <div class="card-icon-wrap icon-network">🔗</div>
                <h3>企业组网服务</h3>
                <p class="card-desc">为企业办公环境提供专业的网络规划、搭建与运维服务，保障网络安全稳定运行。</p>
                <div class="tag-list">
                    <span class="tag">办公组网</span>
                    <span class="tag">VPN搭建</span>
                    <span class="tag">网络安全</span>
                    <span class="tag">无线覆盖</span>
                    <span class="tag">运维支持</span>
                </div>
                <a href="#cta" class="card-link">
                    了解详情
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
                </a>
            </div>
        </div>
    </div>
</section>

<!-- ==================== 核心优势 ==================== -->
<section class="section features-section" id="features">
    <div class="section-inner">
        <div class="section-header">
            <span class="section-label">为什么选择我们</span>
            <h2 class="section-title">专业·可靠·高效</h2>
            <p class="section-subtitle">我们以技术为核心，以服务为根本，助力企业实现数字化飞跃。</p>
        </div>
        <div class="features-grid">
            <div class="feature-item">
                <span class="feature-dot dot-blue"></span>
                <div>
                    <h4>资深技术团队</h4>
                    <p>核心成员来自一线互联网企业，平均10年以上开发经验，技术栈覆盖全面。</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-dot dot-purple"></span>
                <div>
                    <h4>敏捷交付流程</h4>
                    <p>采用Scrum敏捷开发模式，快速迭代，确保项目按时高质量交付。</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-dot dot-green"></span>
                <div>
                    <h4>全方位售后支持</h4>
                    <p>提供7×24小时技术支持，系统上线后持续跟踪优化，让您无后顾之忧。</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-dot dot-orange"></span>
                <div>
                    <h4>数据安全保障</h4>
                    <p>严格的数据加密与权限管理机制，确保企业数据安全合规，符合国家标准。</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-dot dot-blue"></span>
                <div>
                    <h4>行业经验丰富</h4>
                    <p>服务覆盖金融、医疗、制造、零售等多个行业，深刻理解各领域业务逻辑。</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-dot dot-green"></span>
                <div>
                    <h4>高性价比方案</h4>
                    <p>灵活的服务模式与透明的报价体系，为企业提供最具性价比的数字化方案。</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- ==================== 合作流程 ==================== -->
<section class="section" id="process">
    <div class="section-inner">
        <div class="section-header">
            <span class="section-label">合作流程</span>
            <h2 class="section-title">简单四步，轻松启动项目</h2>
            <p class="section-subtitle">规范化的项目流程，确保每一步都清晰透明。</p>
        </div>
        <div class="process-steps">
            <div class="process-step">
                <span class="step-number">01</span>
                <h4>需求沟通</h4>
                <p>深入了解业务需求，梳理功能清单与项目目标，形成需求文档。</p>
            </div>
            <div class="process-step">
                <span class="step-number">02</span>
                <h4>方案设计</h4>
                <p>输出技术方案与原型设计，确认技术架构、UI风格及项目排期。</p>
            </div>
            <div class="process-step">
                <span class="step-number">03</span>
                <h4>开发交付</h4>
                <p>敏捷迭代开发，阶段性验收，确保每个里程碑高质量完成。</p>
            </div>
            <div class="process-step">
                <span class="step-number">04</span>
                <h4>上线运维</h4>
                <p>系统部署上线，提供培训与持续运维支持，保障稳定运行。</p>
            </div>
        </div>
    </div>
</section>

<!-- ==================== CTA ==================== -->
<section class="section cta-section" id="cta">
    <div class="section-inner">
        <h2>准备好开启数字化升级了吗？</h2>
        <p>立即联系我们，获取免费方案咨询与技术评估，专业团队为您量身定制最优解决方案。</p>
        <a href="/contact/" class="btn btn-cta-white">📩 立即免费咨询</a>
        <p style="margin-top:1.2rem;font-size:0.85rem;color:rgba(255,255,255,0.5);">或致电：15928679486 · 工作日 9:00-18:00</p>
    </div>
</section>

<!-- ==================== 页脚 ==================== -->
<footer class="footer">
    <div class="footer-inner">
        <div class="footer-brand">
            <span class="logo-icon">G</span>
            <h4>成都极智软创科技</h4>
            <p style="font-size:0.84rem;">专业软件企业服务商，助力企业数字化升级。</p>
        </div>
        <div class="footer-links">
            <div class="footer-col">
                <h5>服务项目</h5>
                <a href="#services">软件开发</a>
                <a href="#services">IT咨询</a>
                <a href="#services">网站建设</a>
                <a href="#services">企业组网</a>
            </div>
            <div class="footer-col">
                <h5>关于我们</h5>
                <a href="#">公司介绍</a>
                <a href="#">客户案例</a>
                <a href="/job/">加入我们</a>
                <a href="/contact/">联系方式</a>
            </div>
            <div class="footer-col">
                <h5>联系方式</h5>
                <a href="mailto:juncke@163.com">juncke@163.com</a>
                <a href="#">15928679486</a>
                <a href="#">成都市高新区科技园</a>
            </div>
        </div>
    </div>
    <div class="footer-bottom">
        <!-- &copy; 2026 成都极智软创科技. All rights reserved. -->
        Copyright © 2009-{{new Date().getFullYear()}} 成都极智软创科技 为客户创造价值
    </div>
</footer>

<!-- ==================== 脚本 ==================== -->
<script setup>
import { onMounted } from 'vue';

onMounted(() => {
  (function() {
      // 导航栏滚动阴影
      const navbar = document.getElementById('navbar');
      const mobileMenuBtn = document.getElementById('mobileMenuBtn');
      const navLinks = document.getElementById('navLinks');

      window.addEventListener('scroll', function() {
          if (window.scrollY > 20) {
              navbar.classList.add('scrolled');
          } else {
              navbar.classList.remove('scrolled');
          }
      });

      // 移动端菜单切换
      mobileMenuBtn.addEventListener('click', function() {
          navLinks.classList.toggle('open');
      });

      // 点击导航链接后关闭移动菜单
      navLinks.querySelectorAll('a').forEach(function(link) {
          link.addEventListener('click', function() {
              navLinks.classList.remove('open');
          });
      });

      // 点击页面其他区域关闭菜单
      document.addEventListener('click', function(e) {
          if (!navbar.contains(e.target)) {
              navLinks.classList.remove('open');
          }
      });

      // 卡片 hover 触摸设备兼容
      const cards = document.querySelectorAll('.service-card');
      cards.forEach(function(card) {
          card.addEventListener('touchstart', function() {
              cards.forEach(c => c.style.transform = '');
              card.style.transform = 'translateY(-4px)';
          }, { passive: true });
      });

      // 平滑滚动到锚点（兼容处理）
      document.querySelectorAll('a[href^="#"]').forEach(function(anchor) {
          anchor.addEventListener('click', function(e) {
              const href = this.getAttribute('href');
              if (href === '#') return;
              const target = document.querySelector(href);
              if (target) {
                  e.preventDefault();
                  const navHeight = navbar.querySelector('.navbar-inner').offsetHeight;
                  const targetPosition = target.getBoundingClientRect().top + window.pageYOffset -
                      navHeight - 16;
                  window.scrollTo({
                      top: targetPosition,
                      behavior: 'smooth'
                  });
              }
          });
      });

      console.log('🚀 成都极智软创科技 - 专业软件企业服务页面已就绪');
  })();
});
</script>

<style scope>
    :root {
        --primary: #1a6ff5;
        --primary-dark: #0d4eb3;
        --primary-light: #e8f1fe;
        --accent: #00c9a7;
        --dark: #1a1d28;
        --gray-900: #1e2233;
        --gray-800: #2a2f3f;
        --gray-700: #3d4458;
        --gray-600: #5a6275;
        --gray-500: #7b8496;
        --gray-400: #9ca3b3;
        --gray-300: #c5cad5;
        --gray-200: #e2e6ee;
        --gray-100: #f2f5f9;
        --gray-50: #f8fafc;
        --white: #ffffff;
        --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06), 0 1px 2px rgba(0, 0, 0, 0.04);
        --shadow: 0 4px 20px rgba(0, 0, 0, 0.08), 0 1px 3px rgba(0, 0, 0, 0.05);
        --shadow-lg: 0 20px 50px rgba(0, 0, 0, 0.12), 0 6px 20px rgba(0, 0, 0, 0.06);
        --shadow-xl: 0 32px 64px rgba(0, 0, 0, 0.15), 0 8px 24px rgba(0, 0, 0, 0.08);
        --radius-sm: 8px;
        --radius: 12px;
        --radius-lg: 16px;
        --radius-xl: 20px;
        --radius-2xl: 24px;
        --transition: 0.28s cubic-bezier(0.4, 0, 0.2, 1);
        --transition-fast: 0.18s cubic-bezier(0.4, 0, 0.2, 1);
        --font-sans: 'Inter', 'PingFang SC', 'Microsoft YaHei', 'Helvetica Neue', sans-serif;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html {
        scroll-behavior: smooth;
        font-size: 16px;
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
    }

    body {
        font-family: var(--font-sans);
        color: var(--gray-800);
        background-color: var(--white);
        line-height: 1.6;
        overflow-x: hidden;
    }

    /* ============ 导航栏 ============ */
    .navbar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        background: rgba(255, 255, 255, 0.85);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        border-bottom: 1px solid transparent;
        transition: all var(--transition);
        padding: 0 2rem;
    }

    .navbar.scrolled {
        background: rgba(255, 255, 255, 0.95);
        border-bottom-color: var(--gray-200);
        box-shadow: var(--shadow-sm);
    }

    .navbar-inner {
        max-width: 1280px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        height: 68px;
        transition: height var(--transition);
    }

    .navbar.scrolled .navbar-inner {
        height: 56px;
    }

    .nav-logo {
        display: flex;
        align-items: center;
        gap: 10px;
        text-decoration: none;
        font-weight: 700;
        font-size: 1.35rem;
        color: var(--dark);
        letter-spacing: -0.02em;
    }
    .nav-logo .logo-icon {
        width: 40px;
        height: 40px;
        background: linear-gradient(135deg, var(--primary), #3b8bff);
        border-radius: var(--radius-sm);
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-size: 1.2rem;
        font-weight: 800;
        flex-shrink: 0;
        box-shadow: 0 4px 14px rgba(26, 111, 245, 0.35);
    }

    .nav-links {
        display: flex;
        list-style: none;
        gap: 0.25rem;
        align-items: center;
    }
    .nav-links a {
        text-decoration: none;
        color: var(--gray-700);
        font-weight: 500;
        font-size: 0.925rem;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        transition: all var(--transition-fast);
        white-space: nowrap;
        letter-spacing: -0.01em;
    }
    .nav-links a:hover {
        color: var(--primary);
        background: var(--primary-light);
    }
    .nav-cta-btn {
        display: inline-block;
        background: var(--primary);
        color: #fff !important;
        font-weight: 600 !important;
        padding: 0.55rem 1.4rem !important;
        border-radius: 10px !important;
        margin-left: 0.5rem;
        transition: all var(--transition-fast) !important;
        box-shadow: 0 4px 14px rgba(26, 111, 245, 0.3);
        letter-spacing: -0.01em;
    }
    .nav-cta-btn:hover {
        background: var(--primary-dark) !important;
        box-shadow: 0 6px 20px rgba(26, 111, 245, 0.4) !important;
        transform: translateY(-1px);
        color: #fff !important;
    }

    .mobile-menu-btn {
        display: none;
        background: none;
        border: none;
        cursor: pointer;
        padding: 8px;
        color: var(--dark);
        border-radius: 8px;
        transition: background var(--transition-fast);
    }
    .mobile-menu-btn:hover {
        background: var(--gray-100);
    }
    .mobile-menu-btn svg {
        width: 24px;
        height: 24px;
    }

    /* ============ 英雄区域 ============ */
    .hero {
        padding: 140px 2rem 100px;
        background: linear-gradient(180deg, #f8fafd 0%, #eef3fb 40%, #f8fafd 100%);
        position: relative;
        overflow: hidden;
    }
    .hero::before {
        content: '';
        position: absolute;
        top: -300px;
        right: -200px;
        width: 700px;
        height: 700px;
        background: radial-gradient(circle, rgba(26, 111, 245, 0.06) 0%, transparent 70%);
        border-radius: 50%;
        pointer-events: none;
    }
    .hero::after {
        content: '';
        position: absolute;
        bottom: -200px;
        left: -150px;
        width: 500px;
        height: 500px;
        background: radial-gradient(circle, rgba(0, 201, 167, 0.05) 0%, transparent 70%);
        border-radius: 50%;
        pointer-events: none;
    }
    .hero-inner {
        max-width: 1280px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
        display: flex;
        align-items: center;
        gap: 3rem;
        flex-wrap: wrap;
    }
    .hero-text {
        flex: 1 1 480px;
    }
    .hero-badge {
        display: inline-block;
        background: var(--primary-light);
        color: var(--primary);
        font-weight: 600;
        font-size: 0.85rem;
        padding: 0.4rem 1rem;
        border-radius: 50px;
        margin-bottom: 1.2rem;
        letter-spacing: 0.02em;
    }
    .hero-text h1 {
        font-size: clamp(2.2rem, 4.5vw, 3.2rem);
        font-weight: 800;
        line-height: 1.2;
        color: var(--dark);
        letter-spacing: -0.03em;
        margin-bottom: 1rem;
    }
    .hero-text h1 .highlight {
        background: linear-gradient(135deg, var(--primary), #3b8bff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .hero-text p {
        font-size: 1.15rem;
        color: var(--gray-600);
        max-width: 520px;
        margin-bottom: 2rem;
        line-height: 1.7;
    }
    .hero-buttons {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }
    .btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.75rem 1.8rem;
        font-weight: 600;
        font-size: 0.95rem;
        border-radius: 10px;
        text-decoration: none;
        cursor: pointer;
        transition: all var(--transition-fast);
        letter-spacing: -0.01em;
        white-space: nowrap;
        border: none;
        font-family: inherit;
    }
    .btn-primary {
        background: var(--primary);
        color: #fff;
        box-shadow: 0 6px 22px rgba(26, 111, 245, 0.35);
    }
    .btn-primary:hover {
        background: var(--primary-dark);
        box-shadow: 0 8px 28px rgba(26, 111, 245, 0.45);
        transform: translateY(-2px);
    }
    .btn-outline {
        background: var(--white);
        color: var(--primary);
        border: 2px solid var(--gray-200);
    }
    .btn-outline:hover {
        border-color: var(--primary);
        background: var(--primary-light);
        transform: translateY(-2px);
    }
    .hero-visual {
        flex: 1 1 400px;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .hero-card-stack {
        position: relative;
        width: 320px;
        height: 380px;
    }
    .hero-floating-card {
        position: absolute;
        background: #fff;
        border-radius: var(--radius-lg);
        padding: 1.5rem;
        box-shadow: var(--shadow-lg);
        transition: all var(--transition);
    }
    .hero-floating-card.card-1 {
        top: 0;
        left: 10px;
        width: 260px;
        z-index: 3;
        animation: floatCard1 5s ease-in-out infinite;
    }
    .hero-floating-card.card-2 {
        top: 100px;
        right: 0;
        width: 220px;
        z-index: 2;
        animation: floatCard2 5s ease-in-out infinite;
    }
    .hero-floating-card.card-3 {
        bottom: 0;
        left: 30px;
        width: 240px;
        z-index: 1;
        animation: floatCard3 5s ease-in-out infinite;
    }
    @keyframes floatCard1 {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-14px);
        }
    }
    @keyframes floatCard2 {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-10px);
        }
    }
    @keyframes floatCard3 {
        0%,
        100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-8px);
        }
    }
    .hero-floating-card .card-icon {
        width: 44px;
        height: 44px;
        border-radius: var(--radius-sm);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.3rem;
        margin-bottom: 0.8rem;
    }
    .card-icon.blue {
        background: #e8f1fe;
        color: #1a6ff5;
    }
    .card-icon.green {
        background: #e6f9f5;
        color: #00c9a7;
    }
    .card-icon.purple {
        background: #f3edff;
        color: #7c3aed;
    }
    .hero-floating-card h4 {
        font-size: 0.9rem;
        font-weight: 700;
        color: var(--dark);
        margin-bottom: 0.3rem;
    }
    .hero-floating-card span {
        font-size: 0.78rem;
        color: var(--gray-500);
    }

    /* ============ 通用区块 ============ */
    .section {
        padding: 90px 2rem;
    }
    .section-inner {
        max-width: 1280px;
        margin: 0 auto;
    }
    .section-label {
        display: inline-block;
        font-weight: 600;
        font-size: 0.85rem;
        color: var(--primary);
        background: var(--primary-light);
        padding: 0.35rem 1rem;
        border-radius: 50px;
        letter-spacing: 0.03em;
        margin-bottom: 0.8rem;
    }
    .section-title {
        font-size: clamp(1.8rem, 3vw, 2.4rem);
        font-weight: 800;
        color: var(--dark);
        letter-spacing: -0.025em;
        margin-bottom: 0.8rem;
        line-height: 1.25;
    }
    .section-subtitle {
        font-size: 1.05rem;
        color: var(--gray-500);
        max-width: 600px;
        margin-bottom: 3rem;
        line-height: 1.65;
    }
    .section-header {
        text-align: center;
        margin-bottom: 1rem;
    }
    .section-header .section-subtitle {
        margin-left: auto;
        margin-right: auto;
    }

    /* ============ 服务卡片网格 ============ */
    .services-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }
    .service-card {
        background: #fff;
        border-radius: var(--radius-xl);
        padding: 2rem 1.8rem;
        border: 1px solid var(--gray-200);
        transition: all var(--transition);
        cursor: pointer;
        position: relative;
        overflow: hidden;
        box-shadow: var(--shadow-sm);
    }
    .service-card:hover {
        border-color: transparent;
        box-shadow: var(--shadow-xl);
        transform: translateY(-4px);
    }
    .service-card::after {
        content: '';
        position: absolute;
        top: -40px;
        right: -40px;
        width: 100px;
        height: 100px;
        border-radius: 50%;
        opacity: 0;
        transition: all var(--transition);
        pointer-events: none;
    }
    .service-card.card-dev::after {
        background: radial-gradient(circle, rgba(26, 111, 245, 0.08) 0%, transparent 70%);
    }
    .service-card.card-consult::after {
        background: radial-gradient(circle, rgba(124, 58, 237, 0.08) 0%, transparent 70%);
    }
    .service-card.card-web::after {
        background: radial-gradient(circle, rgba(0, 201, 167, 0.08) 0%, transparent 70%);
    }
    .service-card.card-network::after {
        background: radial-gradient(circle, rgba(245, 158, 11, 0.08) 0%, transparent 70%);
    }
    .service-card:hover::after {
        opacity: 1;
        top: -20px;
        right: -20px;
        width: 160px;
        height: 160px;
    }
    .service-card .card-icon-wrap {
        width: 56px;
        height: 56px;
        border-radius: var(--radius);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.6rem;
        margin-bottom: 1.2rem;
        position: relative;
        z-index: 1;
        transition: all var(--transition);
    }
    .icon-saas {
        background: #ffe4e6;
        color: #f43f5e;
    }
    .icon-dev {
        background: #e8f1fe;
        color: #1a6ff5;
    }
    .icon-consult {
        background: #f3edff;
        color: #7c3aed;
    }
    .icon-web {
        background: #e6f9f5;
        color: #00c9a7;
    }
    .icon-network {
        background: #fff7e6;
        color: #f59e0b;
    }
    .service-card:hover .card-icon-wrap {
        transform: scale(1.08);
    }
    .service-card h3 {
        font-size: 1.2rem;
        font-weight: 700;
        color: var(--dark);
        margin-bottom: 0.5rem;
        position: relative;
        z-index: 1;
        letter-spacing: -0.015em;
    }
    .service-card .card-desc {
        font-size: 0.9rem;
        color: var(--gray-500);
        margin-bottom: 1rem;
        position: relative;
        z-index: 1;
        line-height: 1.55;
    }
    .service-card .tag-list {
        display: flex;
        flex-wrap: wrap;
        gap: 0.4rem;
        position: relative;
        z-index: 1;
    }
    .tag {
        display: inline-block;
        font-size: 0.78rem;
        font-weight: 500;
        padding: 0.3rem 0.75rem;
        border-radius: 50px;
        background: var(--gray-100);
        color: var(--gray-600);
        transition: all var(--transition-fast);
        letter-spacing: -0.01em;
    }
    .service-card:hover .tag {
        background: #fff;
        box-shadow: var(--shadow-sm);
    }
    .service-card:hover a.tag:hover {
      background: var(--gray-100);
    }
    .card-link {
        display: inline-flex;
        align-items: center;
        gap: 0.35rem;
        margin-top: 1.2rem;
        font-weight: 600;
        font-size: 0.9rem;
        color: var(--primary);
        text-decoration: none;
        position: relative;
        z-index: 1;
        transition: all var(--transition-fast);
    }
    .card-link svg {
        transition: transform var(--transition-fast);
    }
    .service-card:hover .card-link svg {
        transform: translateX(3px);
    }

    /* ============ 特性亮点 ============ */
    .features-section {
        background: var(--gray-50);
    }
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.2rem;
    }
    .feature-item {
        display: flex;
        gap: 1rem;
        align-items: flex-start;
        padding: 1.5rem;
        border-radius: var(--radius-lg);
        background: #fff;
        border: 1px solid var(--gray-200);
        transition: all var(--transition);
    }
    .feature-item:hover {
        box-shadow: var(--shadow);
        border-color: transparent;
    }
    .feature-dot {
        width: 12px;
        height: 12px;
        border-radius: 50%;
        flex-shrink: 0;
        margin-top: 4px;
    }
    .dot-blue {
        background: #1a6ff5;
        box-shadow: 0 0 0 6px rgba(26, 111, 245, 0.15);
    }
    .dot-purple {
        background: #7c3aed;
        box-shadow: 0 0 0 6px rgba(124, 58, 237, 0.15);
    }
    .dot-green {
        background: #00c9a7;
        box-shadow: 0 0 0 6px rgba(0, 201, 167, 0.15);
    }
    .dot-orange {
        background: #f59e0b;
        box-shadow: 0 0 0 6px rgba(245, 158, 11, 0.15);
    }
    .feature-item h4 {
        font-size: 0.95rem;
        font-weight: 700;
        color: var(--dark);
        margin-bottom: 0.3rem;
        letter-spacing: -0.01em;
    }
    .feature-item p {
        font-size: 0.84rem;
        color: var(--gray-500);
        line-height: 1.5;
    }

    /* ============ 流程 ============ */
    .process-steps {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1rem;
        counter-reset: step;
    }
    .process-step {
        text-align: center;
        padding: 2rem 1.2rem;
        border-radius: var(--radius-xl);
        background: #fff;
        border: 1px solid var(--gray-200);
        position: relative;
        transition: all var(--transition);
    }
    .process-step:hover {
        box-shadow: var(--shadow-lg);
        border-color: transparent;
        transform: translateY(-3px);
    }
    .step-number {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 48px;
        height: 48px;
        border-radius: 50%;
        font-weight: 700;
        font-size: 1.1rem;
        margin-bottom: 1rem;
        background: var(--primary-light);
        color: var(--primary);
        transition: all var(--transition);
    }
    .process-step:hover .step-number {
        background: var(--primary);
        color: #fff;
        box-shadow: 0 8px 24px rgba(26, 111, 245, 0.35);
    }
    .process-step h4 {
        font-weight: 700;
        color: var(--dark);
        margin-bottom: 0.4rem;
        letter-spacing: -0.01em;
    }
    .process-step p {
        font-size: 0.84rem;
        color: var(--gray-500);
        line-height: 1.5;
    }

    /* ============ CTA ============ */
    .cta-section {
        background: linear-gradient(135deg, #0d1b3e 0%, #132752 40%, #0f2245 100%);
        color: #fff;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .cta-section::before {
        content: '';
        position: absolute;
        top: -100px;
        right: -100px;
        width: 400px;
        height: 400px;
        background: radial-gradient(circle, rgba(26, 111, 245, 0.25) 0%, transparent 70%);
        border-radius: 50%;
        pointer-events: none;
    }
    .cta-section .section-inner {
        position: relative;
        z-index: 1;
    }
    .cta-section h2 {
        font-size: clamp(1.8rem, 3vw, 2.4rem);
        font-weight: 800;
        letter-spacing: -0.025em;
        margin-bottom: 0.8rem;
    }
    .cta-section p {
        font-size: 1.05rem;
        color: rgba(255, 255, 255, 0.7);
        max-width: 550px;
        margin: 0 auto 2rem;
        line-height: 1.65;
    }
    .btn-cta-white {
        background: #fff;
        color: var(--dark);
        font-weight: 700;
        box-shadow: 0 8px 28px rgba(0, 0, 0, 0.25);
    }
    .btn-cta-white:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 36px rgba(0, 0, 0, 0.35);
        background: #f0f4ff;
    }

    /* ============ 页脚 ============ */
    .footer {
        background: var(--gray-900);
        color: var(--gray-400);
        padding: 50px 2rem 30px;
    }
    .footer-inner {
        max-width: 1280px;
        margin: 0 auto;
        display: flex;
        flex-wrap: wrap;
        gap: 3rem;
        justify-content: space-between;
    }
    .footer-brand {
        flex: 1 1 240px;
    }
    .footer-brand .logo-icon {
        width: 36px;
        height: 36px;
        background: linear-gradient(135deg, var(--primary), #3b8bff);
        border-radius: 8px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-weight: 800;
        font-size: 1rem;
        margin-bottom: 1rem;
    }
    .footer-brand h4 {
        color: #fff;
        font-weight: 700;
        margin-bottom: 0.5rem;
        letter-spacing: -0.01em;
    }
    .footer-links {
        display: flex;
        gap: 3rem;
        flex-wrap: wrap;
    }
    .footer-col h5 {
        color: #fff;
        font-weight: 600;
        margin-bottom: 0.8rem;
        font-size: 0.9rem;
        letter-spacing: -0.01em;
    }
    .footer-col a {
        display: block;
        color: var(--gray-400);
        text-decoration: none;
        font-size: 0.84rem;
        padding: 0.25rem 0;
        transition: color var(--transition-fast);
    }
    .footer-col a:hover {
        color: #fff;
    }
    .footer-bottom {
        max-width: 1280px;
        margin: 2rem auto 0;
        padding-top: 1.5rem;
        border-top: 1px solid rgba(255, 255, 255, 0.08);
        text-align: center;
        font-size: 0.8rem;
        color: var(--gray-500);
    }

    /* ============ 响应式 ============ */
    @media (max-width: 900px) {
        .nav-links {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            flex-direction: column;
            background: #fff;
            border-bottom: 1px solid var(--gray-200);
            padding: 1rem 2rem;
            box-shadow: var(--shadow-lg);
            gap: 0.3rem;
        }
        .nav-links.open {
            display: flex;
        }
        .mobile-menu-btn {
            display: block;
        }
        .hero-inner {
            flex-direction: column;
            text-align: center;
        }
        .hero-text p {
            margin-left: auto;
            margin-right: auto;
        }
        .hero-buttons {
            justify-content: center;
        }
        .hero-card-stack {
            width: 260px;
            height: 300px;
        }
        .hero-floating-card.card-1 {
            width: 210px;
            left: 0;
        }
        .hero-floating-card.card-2 {
            width: 180px;
            right: 0;
        }
        .hero-floating-card.card-3 {
            width: 190px;
            left: 15px;
        }
        .section {
            padding: 60px 1.2rem;
        }
        .footer-inner {
            flex-direction: column;
            gap: 2rem;
        }
        .footer-links {
            gap: 1.5rem;
        }
    }
    @media (max-width: 480px) {
        .navbar {
            padding: 0 1rem;
        }
        .services-grid {
            grid-template-columns: 1fr;
        }
        .hero {
            padding: 120px 1rem 60px;
        }
        .hero-card-stack {
            display: none;
        }
        .hero-visual {
            flex: 0 0 auto;
        }
        .btn {
            padding: 0.65rem 1.3rem;
            font-size: 0.88rem;
        }
    }
</style>